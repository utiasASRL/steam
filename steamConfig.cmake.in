# - Config file for the libpointmatcher package
# It defines the following variables
#  Steam_INCLUDE_DIRS - include directories for pointmatcher
#  Steam_LIBRARIES    - libraries to link against
 
# Compute paths
get_filename_component(STEAM_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(steam_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
set(steam_LIBRARIES "@STEAM_LIB@;@EXTERNAL_LIBS@")
